<% provide(:title, "Edit Tags") %>
<h1 class="text-center">edit tags</h1>
<div class="row text-center">
	<%= will_paginate %>
</div>
<div class="row">
	<ul class=admin-list>
		<% @tags.each do |tag| %>
			<li>
				<div class="col-md-7 col-md-offset-1 col-sm-6 col-sm-offset-1 col-xs-12">
					<%= tag.name %>
				</div>
				<div class="col-md-3 col-sm-4 col-xs-12 section-buttons">
					<%= link_to "Delete", tag, method: :delete, class: "btn btn-default form-button delete-button" %>
					<%= link_to "Update", edit_tag_path(:id => tag.id), class: "btn btn-default form-button delete-button" %>
				</div>
			</li>
			<div class="">
				<div class="tapered-line section-tapered-line"></div>
			</div>
		<% end %>
	</ul>
</div>
<div class="row text-center">
	<%= will_paginate %>
</div>
